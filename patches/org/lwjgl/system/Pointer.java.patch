--- a/org\lwjgl\system\Pointer.java
+++ b/org\lwjgl\system\Pointer.java
@@ -0,0 +1,10 @@
+package org.lwjgl.system;
+
+/** Stub of {@code org.lwjgl.system.Pointer}. Just needs to exist so callers compile. */
+public interface Pointer {
+    long NULL = 0L;
+    long address();
+    interface Default extends Pointer {
+        @Override default long address() { return 0L; }
+    }
+}

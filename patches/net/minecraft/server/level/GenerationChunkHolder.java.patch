diff --git a/net/minecraft/server/level/GenerationChunkHolder.java b/net/minecraft/server/level/GenerationChunkHolder.java
index 552b8d5..eab7ac2 100644
--- a/net/minecraft/server/level/GenerationChunkHolder.java
+++ b/net/minecraft/server/level/GenerationChunkHolder.java
@@ -9,23 +9,30 @@ import java.util.concurrent.atomic.AtomicInteger;
 import java.util.concurrent.atomic.AtomicReference;
 import java.util.concurrent.atomic.AtomicReferenceArray;
 import javax.annotation.Nullable;
+import net.minecraft.core.BlockPos;
 import net.minecraft.CrashReport;
 import net.minecraft.ReportedException;
 import net.minecraft.server.MinecraftServer;
 import net.minecraft.util.StaticCache2D;
 import net.minecraft.util.VisibleForDebug;
 import net.minecraft.world.level.ChunkPos;
+import net.minecraft.world.level.block.Blocks;
+import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.level.chunk.ChunkAccess;
 import net.minecraft.world.level.chunk.ImposterProtoChunk;
 import net.minecraft.world.level.chunk.ProtoChunk;
 import net.minecraft.world.level.chunk.status.ChunkStatus;
 import net.minecraft.world.level.chunk.status.ChunkStep;
+import net.minecraft.world.level.levelgen.Heightmap;
+import top.steve3184.webmc.web.BuildFlags;
 
 public abstract class GenerationChunkHolder {
     private static final List<ChunkStatus> CHUNK_STATUSES = ChunkStatus.getStatusList();
     private static final ChunkResult<ChunkAccess> NOT_DONE_YET = ChunkResult.error("Not done yet");
     public static final ChunkResult<ChunkAccess> UNLOADED_CHUNK = ChunkResult.error("Unloaded chunk");
     public static final CompletableFuture<ChunkResult<ChunkAccess>> UNLOADED_CHUNK_FUTURE = CompletableFuture.completedFuture(UNLOADED_CHUNK);
+    private static int webNullChunkResultLogs;
+    private static int webScheduleDiagLogs;
     protected final ChunkPos pos;
     @Nullable
     private volatile ChunkStatus highestAllowedStatus;
@@ -44,15 +51,20 @@ public abstract class GenerationChunkHolder {
 
     public CompletableFuture<ChunkResult<ChunkAccess>> scheduleChunkGenerationTask(ChunkStatus p_342080_, ChunkMap p_343602_) {
         if (this.isStatusDisallowed(p_342080_)) {
+            this.logWebScheduleDiag("reject", p_342080_, null);
             return UNLOADED_CHUNK_FUTURE;
         } else {
             CompletableFuture<ChunkResult<ChunkAccess>> completablefuture = this.getOrCreateFuture(p_342080_);
             if (completablefuture.isDone()) {
+                this.logWebScheduleDiag("done", p_342080_, completablefuture);
                 return completablefuture;
             } else {
                 ChunkGenerationTask chunkgenerationtask = this.task.get();
                 if (chunkgenerationtask == null || p_342080_.isAfter(chunkgenerationtask.targetStatus)) {
+                    this.logWebScheduleDiag("reschedule", p_342080_, completablefuture);
                     this.rescheduleChunkTask(p_343602_, p_342080_);
+                } else {
+                    this.logWebScheduleDiag("pending", p_342080_, completablefuture);
                 }
 
                 return completablefuture;
@@ -66,21 +78,153 @@ public abstract class GenerationChunkHolder {
         } else {
             return this.acquireStatusBump(p_344844_.targetStatus()) ? p_342173_.applyStep(this, p_344844_, p_343026_).handle((p_343850_, p_344393_) -> {
                 if (p_344393_ != null) {
+                    System.err.println(
+                        "[mc-web/chunkgen] applyStep failed status="
+                            + p_344844_.targetStatus()
+                            + " pos="
+                            + this.pos
+                            + " cause="
+                            + p_344393_
+                    );
+                    p_344393_.printStackTrace();
+                    if (BuildFlags.WEB_RUNTIME && isWebRecoverableChunkGenFailure(p_344393_)) {
+                        ChunkAccess fallback = this.getFallbackChunkForFailedStep(p_344844_.targetStatus());
+                        if (fallback != null) {
+                            ensureWebFallbackTerrain(fallback, p_344844_.targetStatus());
+                            System.err.println(
+                                "[mc-web/chunkgen] web runtime fallback terrain status="
+                                    + fallback.getPersistedStatus()
+                                    + " for target="
+                                    + p_344844_.targetStatus()
+                            );
+                            return ChunkResult.of(fallback);
+                        }
+                        System.err.println("[mc-web/chunkgen] web runtime fallback unavailable; returning UNLOADED chunk");
+                        return UNLOADED_CHUNK;
+                    }
                     CrashReport crashreport = CrashReport.forThrowable(p_344393_, "Exception chunk generation/loading");
                     MinecraftServer.setFatalException(new ReportedException(crashreport));
+                    return UNLOADED_CHUNK;
                 } else {
                     this.completeFuture(p_344844_.targetStatus(), p_343850_);
+                    return ChunkResult.of(p_343850_);
                 }
-
-                return ChunkResult.of(p_343850_);
             }) : this.getOrCreateFuture(p_344844_.targetStatus());
         }
     }
 
+    private static boolean isWebRecoverableChunkGenFailure(Throwable t) {
+        String msg = String.valueOf(t);
+        return msg.contains("Suspension point reached from non-threading context");
+    }
+
+    @Nullable
+    private ChunkAccess getFallbackChunkForFailedStep(ChunkStatus target) {
+        ChunkStatus cursor = target.getParent();
+        while (cursor != null) {
+            ChunkAccess chunk = this.getChunkIfPresentUnchecked(cursor);
+            if (chunk != null) {
+                return chunk;
+            }
+            if (cursor == ChunkStatus.EMPTY) {
+                break;
+            }
+            cursor = cursor.getParent();
+        }
+        return this.getChunkIfPresentUnchecked(ChunkStatus.EMPTY);
+    }
+
+    private static void ensureWebFallbackTerrain(ChunkAccess chunk, ChunkStatus target) {
+        if (!BuildFlags.WEB_RUNTIME || target.isBefore(ChunkStatus.NOISE)) {
+            return;
+        }
+
+        int minY = chunk.getMinY();
+        if (chunk.isOutsideBuildHeight(minY + 3)) {
+            return;
+        }
+
+        BlockState bedrock = Blocks.BEDROCK.defaultBlockState();
+        BlockState dirt = Blocks.DIRT.defaultBlockState();
+        BlockState grass = Blocks.GRASS_BLOCK.defaultBlockState();
+        BlockPos.MutableBlockPos pos = new BlockPos.MutableBlockPos();
+        ChunkPos chunkPos = chunk.getPos();
+        Heightmap oceanFloor = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.OCEAN_FLOOR_WG);
+        Heightmap worldSurface = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.WORLD_SURFACE_WG);
+        Heightmap oceanFloorFinal = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.OCEAN_FLOOR);
+        Heightmap worldSurfaceFinal = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.WORLD_SURFACE);
+        Heightmap motionBlocking = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.MOTION_BLOCKING);
+        Heightmap motionBlockingNoLeaves = chunk.getOrCreateHeightmapUnprimed(Heightmap.Types.MOTION_BLOCKING_NO_LEAVES);
+
+        int placed = 0;
+        for (int x = 0; x < 16; x++) {
+            for (int z = 0; z < 16; z++) {
+                placed += setFallbackBlock(chunk, pos, chunkPos, x, minY, z, bedrock, oceanFloor, worldSurface, oceanFloorFinal, worldSurfaceFinal, motionBlocking, motionBlockingNoLeaves);
+                placed += setFallbackBlock(chunk, pos, chunkPos, x, minY + 1, z, dirt, oceanFloor, worldSurface, oceanFloorFinal, worldSurfaceFinal, motionBlocking, motionBlockingNoLeaves);
+                placed += setFallbackBlock(chunk, pos, chunkPos, x, minY + 2, z, dirt, oceanFloor, worldSurface, oceanFloorFinal, worldSurfaceFinal, motionBlocking, motionBlockingNoLeaves);
+                placed += setFallbackBlock(chunk, pos, chunkPos, x, minY + 3, z, grass, oceanFloor, worldSurface, oceanFloorFinal, worldSurfaceFinal, motionBlocking, motionBlockingNoLeaves);
+            }
+        }
+
+        if (chunk instanceof ProtoChunk protoChunk) {
+            protoChunk.setPersistedStatus(target);
+        } else if (chunk instanceof ImposterProtoChunk imposterProtoChunk) {
+            imposterProtoChunk.setPersistedStatus(target);
+        }
+
+        System.err.println(
+            "[mc-web/chunkgen] fallbackTerrain chunk="
+                + chunkPos.x
+                + ","
+                + chunkPos.z
+                + " target="
+                + target
+                + " minY="
+                + minY
+                + " blocks="
+                + placed
+        );
+    }
+
+    private static int setFallbackBlock(
+        ChunkAccess chunk,
+        BlockPos.MutableBlockPos pos,
+        ChunkPos chunkPos,
+        int x,
+        int y,
+        int z,
+        BlockState state,
+        Heightmap... heightmaps
+    ) {
+        pos.set(chunkPos.getBlockX(x), y, chunkPos.getBlockZ(z));
+        chunk.setBlockState(pos, state);
+        for (Heightmap heightmap : heightmaps) {
+            heightmap.update(x, y, z, state);
+        }
+        return 1;
+    }
+
     protected void updateHighestAllowedStatus(ChunkMap p_345117_) {
         ChunkStatus chunkstatus = this.highestAllowedStatus;
         ChunkStatus chunkstatus1 = ChunkLevel.generationStatus(this.getTicketLevel());
         this.highestAllowedStatus = chunkstatus1;
+        if (this.shouldLogWebScheduleDiag()) {
+            webScheduleDiagLogs++;
+            System.out.println(
+                "[mc-web/chunkfuture] allowedStatus pos="
+                    + this.pos.x
+                    + ","
+                    + this.pos.z
+                    + " ticket="
+                    + this.getTicketLevel()
+                    + " old="
+                    + chunkstatus
+                    + " new="
+                    + chunkstatus1
+                    + " latest="
+                    + this.getLatestStatus()
+            );
+        }
         boolean flag = chunkstatus != null && (chunkstatus1 == null || chunkstatus1.isBefore(chunkstatus));
         if (flag) {
             this.failAndClearPendingFuturesBetween(chunkstatus1, chunkstatus);
@@ -256,7 +400,17 @@ public abstract class GenerationChunkHolder {
     @Nullable
     public ChunkAccess getChunkIfPresentUnchecked(ChunkStatus p_342422_) {
         CompletableFuture<ChunkResult<ChunkAccess>> completablefuture = this.futures.get(p_342422_.getIndex());
-        return completablefuture == null ? null : completablefuture.getNow(NOT_DONE_YET).orElse(null);
+        if (completablefuture == null) {
+            return null;
+        } else {
+            ChunkResult<ChunkAccess> chunkresult = completablefuture.getNow(NOT_DONE_YET);
+            if (chunkresult == null) {
+                logWebNullChunkResult("getChunkIfPresentUnchecked", p_342422_, completablefuture);
+                return null;
+            } else {
+                return chunkresult.orElse(null);
+            }
+        }
     }
 
     @Nullable
@@ -278,10 +432,74 @@ public abstract class GenerationChunkHolder {
     @Nullable
     public ChunkStatus getPersistedStatus() {
         CompletableFuture<ChunkResult<ChunkAccess>> completablefuture = this.futures.get(ChunkStatus.EMPTY.getIndex());
-        ChunkAccess chunkaccess = completablefuture == null ? null : completablefuture.getNow(NOT_DONE_YET).orElse(null);
+        ChunkAccess chunkaccess;
+        if (completablefuture == null) {
+            chunkaccess = null;
+        } else {
+            ChunkResult<ChunkAccess> chunkresult = completablefuture.getNow(NOT_DONE_YET);
+            if (chunkresult == null) {
+                logWebNullChunkResult("getPersistedStatus", ChunkStatus.EMPTY, completablefuture);
+                chunkaccess = null;
+            } else {
+                chunkaccess = chunkresult.orElse(null);
+            }
+        }
         return chunkaccess == null ? null : chunkaccess.getPersistedStatus();
     }
 
+    private void logWebNullChunkResult(String p_410915_, ChunkStatus p_410768_, CompletableFuture<ChunkResult<ChunkAccess>> p_410765_) {
+        if (BuildFlags.WEB_RUNTIME && webNullChunkResultLogs < 8) {
+            webNullChunkResultLogs++;
+            System.err.println(
+                "[mc-web/chunkfuture] null chunk result context="
+                    + p_410915_
+                    + " pos="
+                    + this.pos.x
+                    + ","
+                    + this.pos.z
+                    + " status="
+                    + p_410768_
+                    + " futureDone="
+                    + p_410765_.isDone()
+                    + " ticket="
+                    + this.getTicketLevel()
+                    + " highestAllowed="
+                    + this.highestAllowedStatus
+            );
+        }
+    }
+
+    private boolean shouldLogWebScheduleDiag() {
+        return BuildFlags.WEB_RUNTIME
+            && BuildFlags.ENABLE_EXPERIMENTAL_MC_MAIN
+            && this.pos.getChessboardDistance(ChunkPos.ZERO) <= 2
+            && webScheduleDiagLogs < 16;
+    }
+
+    private void logWebScheduleDiag(String p_410977_, ChunkStatus p_411195_, @Nullable CompletableFuture<ChunkResult<ChunkAccess>> p_410702_) {
+        if (this.shouldLogWebScheduleDiag() && p_411195_ == ChunkStatus.FULL) {
+            webScheduleDiagLogs++;
+            System.out.println(
+                "[mc-web/chunkfuture] schedule."
+                    + p_410977_
+                    + " pos="
+                    + this.pos.x
+                    + ","
+                    + this.pos.z
+                    + " requested="
+                    + p_411195_
+                    + " ticket="
+                    + this.getTicketLevel()
+                    + " highestAllowed="
+                    + this.highestAllowedStatus
+                    + " latest="
+                    + this.getLatestStatus()
+                    + " future="
+                    + (p_410702_ == null ? "null" : (p_410702_.isDone() ? "done" : "pending"))
+            );
+        }
+    }
+
     public ChunkPos getPos() {
         return this.pos;
     }
@@ -318,4 +536,4 @@ public abstract class GenerationChunkHolder {
 
         return null;
     }
-}
\ No newline at end of file
+}

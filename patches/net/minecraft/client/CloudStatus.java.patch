--- a/net/minecraft/client/CloudStatus.java
+++ b/net/minecraft/client/CloudStatus.java
@@ -5,6 +5,7 @@
 import net.minecraft.util.StringRepresentable;
 import net.minecraftforge.api.distmarker.Dist;
 import net.minecraftforge.api.distmarker.OnlyIn;
+import top.steve3184.webmc.web.BuildFlags;
 
 @OnlyIn(Dist.CLIENT)
 public enum CloudStatus implements OptionEnum, StringRepresentable {
@@ -12,7 +13,9 @@
     FAST(1, "fast", "options.clouds.fast"),
     FANCY(2, "true", "options.clouds.fancy");
 
-    public static final Codec<CloudStatus> CODEC = StringRepresentable.fromEnum(CloudStatus::values);
+    public static final Codec<CloudStatus> CODEC = BuildFlags.WEB_RUNTIME
+        ? Codec.INT.xmap(CloudStatus::byId, CloudStatus::getId)
+        : StringRepresentable.fromEnum(CloudStatus::values);
     private final int id;
     private final String legacyName;
     private final String key;
@@ -37,4 +40,13 @@
     public String getKey() {
         return this.key;
     }
-}
\ No newline at end of file
+
+    private static CloudStatus byId(int id) {
+        for (CloudStatus value : values()) {
+            if (value.id == id) {
+                return value;
+            }
+        }
+        return FANCY;
+    }
+}

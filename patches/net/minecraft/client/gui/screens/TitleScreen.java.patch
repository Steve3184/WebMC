--- a/net/minecraft/client/gui/screens/TitleScreen.java
+++ b/net/minecraft/client/gui/screens/TitleScreen.java
@@ -40,6 +40,7 @@
 import net.minecraftforge.api.distmarker.Dist;
 import net.minecraftforge.api.distmarker.OnlyIn;
 import org.slf4j.Logger;
+import top.steve3184.webmc.web.BuildFlags;
 
 @OnlyIn(Dist.CLIENT)
 public class TitleScreen extends Screen {
@@ -138,7 +139,7 @@
                 j, this.height - 10, i, 10, COPYRIGHT_TEXT, p_280834_ -> this.minecraft.setScreen(new CreditsAndAttributionScreen(this)), this.font
             )
         );
-        if (this.realmsNotificationsScreen == null) {
+        if (!BuildFlags.WEB_RUNTIME && this.realmsNotificationsScreen == null) {
             this.realmsNotificationsScreen = new RealmsNotificationsScreen();
         }
 
@@ -165,6 +166,19 @@
                 .bounds(this.width / 2 - 100, p_96764_, 200, 20)
                 .build()
         );
+        if (BuildFlags.WEB_RUNTIME) {
+            this.addRenderableWidget(
+                Button.builder(Component.translatable("menu.multiplayer"), p_280833_ -> {
+                    System.err.println("[mc-web] Multiplayer button disabled in browser runtime");
+                }).bounds(this.width / 2 - 100, p_96764_ + p_96765_, 200, 20).build()
+            ).active = false;
+            this.addRenderableWidget(
+                Button.builder(Component.translatable("menu.online"), p_325369_ -> {
+                    System.err.println("[mc-web] Realms button disabled in browser runtime");
+                }).bounds(this.width / 2 - 100, p_96764_ + p_96765_ * 2, 200, 20).build()
+            ).active = false;
+            return p_96764_ + p_96765_ * 2;
+        }
         Component component = this.getMultiplayerDisabledReason();
         boolean flag = component == null;
         Tooltip tooltip = component != null ? Tooltip.create(component) : null;
@@ -337,4 +351,4 @@
 
         this.minecraft.setScreen(this);
     }
-}
\ No newline at end of file
+}

diff --git a/net/minecraft/world/entity/LivingEntity.java b/net/minecraft/world/entity/LivingEntity.java
index e43a2d5..70edb65 100644
--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -138,9 +138,12 @@ import net.minecraft.world.waypoints.Waypoint;
 import net.minecraft.world.waypoints.WaypointTransmitter;
 import org.jetbrains.annotations.Contract;
 import org.slf4j.Logger;
+import top.steve3184.webmc.web.BuildFlags;
+import top.steve3184.webmc.web.WebDiagnostics;
 
 public abstract class LivingEntity extends Entity implements Attackable, WaypointTransmitter {
     private static final Logger LOGGER = LogUtils.getLogger();
+    private static int webAiStepExceptionLogs;
     private static final String TAG_ACTIVE_EFFECTS = "active_effects";
     public static final String TAG_ATTRIBUTES = "attributes";
     public static final String TAG_SLEEPING_POS = "sleeping_pos";
@@ -169,6 +172,8 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
     private static final int FREE_FALL_EVENTS_PER_ELYTRA_BREAK = 2;
     public static final float BASE_JUMP_POWER = 0.42F;
     private static final double MAX_LINE_OF_SIGHT_TEST_RANGE = 128.0;
+    @SuppressWarnings("unused")
+    private static final EntityDataAccessor<Pose> WEBMC_FORCE_ENTITY_SYNCHER_INIT = Entity.DATA_POSE;
     protected static final int LIVING_ENTITY_FLAG_IS_USING = 1;
     protected static final int LIVING_ENTITY_FLAG_OFF_HAND = 2;
     protected static final int LIVING_ENTITY_FLAG_SPIN_ATTACK = 4;
@@ -260,14 +265,23 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
 
     protected LivingEntity(EntityType<? extends LivingEntity> p_20966_, Level p_20967_) {
         super(p_20966_, p_20967_);
+        if (WebDiagnostics.enabled()) {
+            WebDiagnostics.log("[mc-web/ctor] LivingEntity.afterSuper type=" + p_20966_);
+            WebDiagnostics.log("[mc-web/ctor] LivingEntity.beforeAttributes");
+        }
         this.attributes = new AttributeMap(DefaultAttributes.getSupplier(p_20966_));
+        WebDiagnostics.log("[mc-web/ctor] LivingEntity.afterAttributes");
         this.setHealth(this.getMaxHealth());
+        WebDiagnostics.log("[mc-web/ctor] LivingEntity.afterSetHealth");
         this.equipment = this.createEquipment();
+        WebDiagnostics.log("[mc-web/ctor] LivingEntity.afterCreateEquipment");
         this.blocksBuilding = true;
         this.reapplyPosition();
         this.setYRot((float)(Math.random() * (float) (Math.PI * 2)));
         this.yHeadRot = this.getYRot();
+        WebDiagnostics.log("[mc-web/ctor] LivingEntity.beforeBrain");
         this.brain = this.makeBrain(EMPTY_BRAIN);
+        WebDiagnostics.log("[mc-web/ctor] LivingEntity.end");
     }
 
     @Contract(
@@ -2498,117 +2512,139 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
 
     @Override
     public void tick() {
-        super.tick();
-        this.updatingUsingItem();
-        this.updateSwimAmount();
-        if (!this.level().isClientSide) {
-            int i = this.getArrowCount();
-            if (i > 0) {
-                if (this.removeArrowTime <= 0) {
-                    this.removeArrowTime = 20 * (30 - i);
+        String webTickStage = "start";
+        try {
+            super.tick();
+            webTickStage = "after-super-tick";
+            this.updatingUsingItem();
+            webTickStage = "after-updatingUsingItem";
+            this.updateSwimAmount();
+            webTickStage = "after-updateSwimAmount";
+            if (!this.level().isClientSide) {
+                int i = this.getArrowCount();
+                if (i > 0) {
+                    if (this.removeArrowTime <= 0) {
+                        this.removeArrowTime = 20 * (30 - i);
+                    }
+
+                    this.removeArrowTime--;
+                    if (this.removeArrowTime <= 0) {
+                        this.setArrowCount(i - 1);
+                    }
                 }
 
-                this.removeArrowTime--;
-                if (this.removeArrowTime <= 0) {
-                    this.setArrowCount(i - 1);
+                int j = this.getStingerCount();
+                if (j > 0) {
+                    if (this.removeStingerTime <= 0) {
+                        this.removeStingerTime = 20 * (30 - j);
+                    }
+
+                    this.removeStingerTime--;
+                    if (this.removeStingerTime <= 0) {
+                        this.setStingerCount(j - 1);
+                    }
                 }
-            }
 
-            int j = this.getStingerCount();
-            if (j > 0) {
-                if (this.removeStingerTime <= 0) {
-                    this.removeStingerTime = 20 * (30 - j);
+                this.detectEquipmentUpdates();
+                if (this.tickCount % 20 == 0) {
+                    this.getCombatTracker().recheckStatus();
                 }
 
-                this.removeStingerTime--;
-                if (this.removeStingerTime <= 0) {
-                    this.setStingerCount(j - 1);
+                if (this.isSleeping() && !this.checkBedExists()) {
+                    this.stopSleeping();
                 }
             }
 
-            this.detectEquipmentUpdates();
-            if (this.tickCount % 20 == 0) {
-                this.getCombatTracker().recheckStatus();
+            webTickStage = "pre-aiStep";
+            if (!this.isRemoved()) {
+                this.aiStep();
             }
+            webTickStage = "after-aiStep";
 
-            if (this.isSleeping() && !this.checkBedExists()) {
-                this.stopSleeping();
+            double d1 = this.getX() - this.xo;
+            double d0 = this.getZ() - this.zo;
+            float f = (float)(d1 * d1 + d0 * d0);
+            float f1 = this.yBodyRot;
+            if (f > 0.0025000002F) {
+                float f2 = (float)Mth.atan2(d0, d1) * (180.0F / (float)Math.PI) - 90.0F;
+                float f3 = Mth.abs(Mth.wrapDegrees(this.getYRot()) - f2);
+                if (95.0F < f3 && f3 < 265.0F) {
+                    f1 = f2 - 180.0F;
+                } else {
+                    f1 = f2;
+                }
             }
-        }
 
-        if (!this.isRemoved()) {
-            this.aiStep();
-        }
-
-        double d1 = this.getX() - this.xo;
-        double d0 = this.getZ() - this.zo;
-        float f = (float)(d1 * d1 + d0 * d0);
-        float f1 = this.yBodyRot;
-        if (f > 0.0025000002F) {
-            float f2 = (float)Mth.atan2(d0, d1) * (180.0F / (float)Math.PI) - 90.0F;
-            float f3 = Mth.abs(Mth.wrapDegrees(this.getYRot()) - f2);
-            if (95.0F < f3 && f3 < 265.0F) {
-                f1 = f2 - 180.0F;
-            } else {
-                f1 = f2;
+            if (this.attackAnim > 0.0F) {
+                f1 = this.getYRot();
             }
-        }
 
-        if (this.attackAnim > 0.0F) {
-            f1 = this.getYRot();
-        }
+            ProfilerFiller profilerfiller = Profiler.get();
+            profilerfiller.push("headTurn");
+            this.tickHeadTurn(f1);
+            profilerfiller.pop();
+            profilerfiller.push("rangeChecks");
 
-        ProfilerFiller profilerfiller = Profiler.get();
-        profilerfiller.push("headTurn");
-        this.tickHeadTurn(f1);
-        profilerfiller.pop();
-        profilerfiller.push("rangeChecks");
+            while (this.getYRot() - this.yRotO < -180.0F) {
+                this.yRotO -= 360.0F;
+            }
 
-        while (this.getYRot() - this.yRotO < -180.0F) {
-            this.yRotO -= 360.0F;
-        }
+            while (this.getYRot() - this.yRotO >= 180.0F) {
+                this.yRotO += 360.0F;
+            }
 
-        while (this.getYRot() - this.yRotO >= 180.0F) {
-            this.yRotO += 360.0F;
-        }
+            while (this.yBodyRot - this.yBodyRotO < -180.0F) {
+                this.yBodyRotO -= 360.0F;
+            }
 
-        while (this.yBodyRot - this.yBodyRotO < -180.0F) {
-            this.yBodyRotO -= 360.0F;
-        }
+            while (this.yBodyRot - this.yBodyRotO >= 180.0F) {
+                this.yBodyRotO += 360.0F;
+            }
 
-        while (this.yBodyRot - this.yBodyRotO >= 180.0F) {
-            this.yBodyRotO += 360.0F;
-        }
+            while (this.getXRot() - this.xRotO < -180.0F) {
+                this.xRotO -= 360.0F;
+            }
 
-        while (this.getXRot() - this.xRotO < -180.0F) {
-            this.xRotO -= 360.0F;
-        }
+            while (this.getXRot() - this.xRotO >= 180.0F) {
+                this.xRotO += 360.0F;
+            }
 
-        while (this.getXRot() - this.xRotO >= 180.0F) {
-            this.xRotO += 360.0F;
-        }
+            while (this.yHeadRot - this.yHeadRotO < -180.0F) {
+                this.yHeadRotO -= 360.0F;
+            }
 
-        while (this.yHeadRot - this.yHeadRotO < -180.0F) {
-            this.yHeadRotO -= 360.0F;
-        }
+            while (this.yHeadRot - this.yHeadRotO >= 180.0F) {
+                this.yHeadRotO += 360.0F;
+            }
 
-        while (this.yHeadRot - this.yHeadRotO >= 180.0F) {
-            this.yHeadRotO += 360.0F;
-        }
+            profilerfiller.pop();
+            if (this.isFallFlying()) {
+                this.fallFlyTicks++;
+            } else {
+                this.fallFlyTicks = 0;
+            }
 
-        profilerfiller.pop();
-        if (this.isFallFlying()) {
-            this.fallFlyTicks++;
-        } else {
-            this.fallFlyTicks = 0;
-        }
+            if (this.isSleeping()) {
+                this.setXRot(0.0F);
+            }
 
-        if (this.isSleeping()) {
-            this.setXRot(0.0F);
+            webTickStage = "refreshDirtyAttributes";
+            this.refreshDirtyAttributes();
+            webTickStage = "elytraAnimationStateTick";
+            this.elytraAnimationState.tick();
+        } catch (Throwable throwable) {
+            if (BuildFlags.WEB_RUNTIME) {
+                System.err.println(
+                    "[mc-web/livingtick] tick caught stage="
+                        + webTickStage
+                        + " "
+                        + throwable.getClass().getName()
+                        + ": "
+                        + throwable.getMessage()
+                );
+            }
+            throw throwable;
         }
-
-        this.refreshDirtyAttributes();
-        this.elytraAnimationState.tick();
     }
 
     private void detectEquipmentUpdates() {
@@ -2707,139 +2743,174 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
     }
 
     public void aiStep() {
-        if (this.noJumpDelay > 0) {
-            this.noJumpDelay--;
-        }
-
-        if (this.isInterpolating()) {
-            this.getInterpolation().interpolate();
-        } else if (!this.canSimulateMovement()) {
-            this.setDeltaMovement(this.getDeltaMovement().scale(0.98));
-        }
+        String webAiStepStage = "start";
+        try {
+            if (this.noJumpDelay > 0) {
+                this.noJumpDelay--;
+            }
 
-        if (this.lerpHeadSteps > 0) {
-            this.lerpHeadRotationStep(this.lerpHeadSteps, this.lerpYHeadRot);
-            this.lerpHeadSteps--;
-        }
+            webAiStepStage = "interpolation";
+            if (this.isInterpolating()) {
+                this.getInterpolation().interpolate();
+            } else if (!this.canSimulateMovement()) {
+                this.setDeltaMovement(this.getDeltaMovement().scale(0.98));
+            }
 
-        this.equipment.tick(this);
-        Vec3 vec3 = this.getDeltaMovement();
-        double d0 = vec3.x;
-        double d1 = vec3.y;
-        double d2 = vec3.z;
-        if (this.getType().equals(EntityType.PLAYER)) {
-            if (vec3.horizontalDistanceSqr() < 9.0E-6) {
-                d0 = 0.0;
-                d2 = 0.0;
+            webAiStepStage = "lerp-head";
+            if (this.lerpHeadSteps > 0) {
+                this.lerpHeadRotationStep(this.lerpHeadSteps, this.lerpYHeadRot);
+                this.lerpHeadSteps--;
             }
-        } else {
-            if (Math.abs(vec3.x) < 0.003) {
-                d0 = 0.0;
+
+            webAiStepStage = "equipment.tick";
+            this.equipment.tick(this);
+            Vec3 vec3 = this.getDeltaMovement();
+            double d0 = vec3.x;
+            double d1 = vec3.y;
+            double d2 = vec3.z;
+            webAiStepStage = "delta-normalize";
+            if (this.getType().equals(EntityType.PLAYER)) {
+                if (vec3.horizontalDistanceSqr() < 9.0E-6) {
+                    d0 = 0.0;
+                    d2 = 0.0;
+                }
+            } else {
+                if (Math.abs(vec3.x) < 0.003) {
+                    d0 = 0.0;
+                }
+
+                if (Math.abs(vec3.z) < 0.003) {
+                    d2 = 0.0;
+                }
             }
 
-            if (Math.abs(vec3.z) < 0.003) {
-                d2 = 0.0;
+            if (Math.abs(vec3.y) < 0.003) {
+                d1 = 0.0;
             }
-        }
 
-        if (Math.abs(vec3.y) < 0.003) {
-            d1 = 0.0;
-        }
+            this.setDeltaMovement(d0, d1, d2);
+            ProfilerFiller profilerfiller = Profiler.get();
+            profilerfiller.push("ai");
+            webAiStepStage = "applyInput";
+            this.applyInput();
+            if (this.isImmobile()) {
+                this.jumping = false;
+                this.xxa = 0.0F;
+                this.zza = 0.0F;
+            } else if (this.isEffectiveAi() && !this.level().isClientSide) {
+                webAiStepStage = "serverAiStep";
+                profilerfiller.push("newAi");
+                this.serverAiStep();
+                profilerfiller.pop();
+            }
 
-        this.setDeltaMovement(d0, d1, d2);
-        ProfilerFiller profilerfiller = Profiler.get();
-        profilerfiller.push("ai");
-        this.applyInput();
-        if (this.isImmobile()) {
-            this.jumping = false;
-            this.xxa = 0.0F;
-            this.zza = 0.0F;
-        } else if (this.isEffectiveAi() && !this.level().isClientSide) {
-            profilerfiller.push("newAi");
-            this.serverAiStep();
             profilerfiller.pop();
-        }
-
-        profilerfiller.pop();
-        profilerfiller.push("jump");
-        if (this.jumping && this.isAffectedByFluids()) {
-            double d3;
-            if (this.isInLava()) {
-                d3 = this.getFluidHeight(FluidTags.LAVA);
-            } else {
-                d3 = this.getFluidHeight(FluidTags.WATER);
-            }
+            profilerfiller.push("jump");
+            webAiStepStage = "jump";
+            if (this.jumping && this.isAffectedByFluids()) {
+                double d3;
+                if (this.isInLava()) {
+                    d3 = this.getFluidHeight(FluidTags.LAVA);
+                } else {
+                    d3 = this.getFluidHeight(FluidTags.WATER);
+                }
 
-            boolean flag = this.isInWater() && d3 > 0.0;
-            double d4 = this.getFluidJumpThreshold();
-            if (!flag || this.onGround() && !(d3 > d4)) {
-                if (!this.isInLava() || this.onGround() && !(d3 > d4)) {
-                    if ((this.onGround() || flag && d3 <= d4) && this.noJumpDelay == 0) {
-                        this.jumpFromGround();
-                        this.noJumpDelay = 10;
+                boolean flag = this.isInWater() && d3 > 0.0;
+                double d4 = this.getFluidJumpThreshold();
+                if (!flag || this.onGround() && !(d3 > d4)) {
+                    if (!this.isInLava() || this.onGround() && !(d3 > d4)) {
+                        if ((this.onGround() || flag && d3 <= d4) && this.noJumpDelay == 0) {
+                            this.jumpFromGround();
+                            this.noJumpDelay = 10;
+                        }
+                    } else {
+                        this.jumpInLiquid(FluidTags.LAVA);
                     }
                 } else {
-                    this.jumpInLiquid(FluidTags.LAVA);
+                    this.jumpInLiquid(FluidTags.WATER);
                 }
             } else {
-                this.jumpInLiquid(FluidTags.WATER);
+                this.noJumpDelay = 0;
             }
-        } else {
-            this.noJumpDelay = 0;
-        }
-
-        profilerfiller.pop();
-        profilerfiller.push("travel");
-        if (this.isFallFlying()) {
-            this.updateFallFlying();
-        }
 
-        AABB aabb = this.getBoundingBox();
-        Vec3 vec31 = new Vec3(this.xxa, this.yya, this.zza);
-        if (this.hasEffect(MobEffects.SLOW_FALLING) || this.hasEffect(MobEffects.LEVITATION)) {
-            this.resetFallDistance();
-        }
-
-        if (this.getControllingPassenger() instanceof Player player && this.isAlive()) {
-            this.travelRidden(player, vec31);
-        } else if (this.canSimulateMovement() && this.isEffectiveAi()) {
-            this.travel(vec31);
-        }
+            profilerfiller.pop();
+            profilerfiller.push("travel");
+            webAiStepStage = "updateFallFlying";
+            if (this.isFallFlying()) {
+                this.updateFallFlying();
+            }
 
-        if (!this.level().isClientSide() || this.isLocalInstanceAuthoritative()) {
-            this.applyEffectsFromBlocks();
-        }
+            AABB aabb = this.getBoundingBox();
+            Vec3 vec31 = new Vec3(this.xxa, this.yya, this.zza);
+            if (this.hasEffect(MobEffects.SLOW_FALLING) || this.hasEffect(MobEffects.LEVITATION)) {
+                this.resetFallDistance();
+            }
 
-        if (this.level().isClientSide()) {
-            this.calculateEntityAnimation(this instanceof FlyingAnimal);
-        }
+            webAiStepStage = "travel";
+            if (this.getControllingPassenger() instanceof Player player && this.isAlive()) {
+                this.travelRidden(player, vec31);
+            } else if (this.canSimulateMovement() && this.isEffectiveAi()) {
+                this.travel(vec31);
+            }
 
-        profilerfiller.pop();
-        if (this.level() instanceof ServerLevel serverlevel) {
-            profilerfiller.push("freezing");
-            if (!this.isInPowderSnow || !this.canFreeze()) {
-                this.setTicksFrozen(Math.max(0, this.getTicksFrozen() - 2));
+            webAiStepStage = "applyEffectsFromBlocks";
+            if (!this.level().isClientSide() || this.isLocalInstanceAuthoritative()) {
+                this.applyEffectsFromBlocks();
             }
 
-            this.removeFrost();
-            this.tryAddFrost();
-            if (this.tickCount % 40 == 0 && this.isFullyFrozen() && this.canFreeze()) {
-                this.hurtServer(serverlevel, this.damageSources().freeze(), 1.0F);
+            webAiStepStage = "calculateEntityAnimation";
+            if (this.level().isClientSide()) {
+                this.calculateEntityAnimation(this instanceof FlyingAnimal);
             }
 
             profilerfiller.pop();
-        }
+            if (this.level() instanceof ServerLevel serverlevel) {
+                webAiStepStage = "freezing";
+                profilerfiller.push("freezing");
+                if (!this.isInPowderSnow || !this.canFreeze()) {
+                    this.setTicksFrozen(Math.max(0, this.getTicksFrozen() - 2));
+                }
 
-        profilerfiller.push("push");
-        if (this.autoSpinAttackTicks > 0) {
-            this.autoSpinAttackTicks--;
-            this.checkAutoSpinAttack(aabb, this.getBoundingBox());
-        }
+                this.removeFrost();
+                this.tryAddFrost();
+                if (this.tickCount % 40 == 0 && this.isFullyFrozen() && this.canFreeze()) {
+                    this.hurtServer(serverlevel, this.damageSources().freeze(), 1.0F);
+                }
 
-        this.pushEntities();
-        profilerfiller.pop();
-        if (this.level() instanceof ServerLevel serverlevel1 && this.isSensitiveToWater() && this.isInWaterOrRain()) {
-            this.hurtServer(serverlevel1, this.damageSources().drown(), 1.0F);
+                profilerfiller.pop();
+            }
+
+            profilerfiller.push("push");
+            webAiStepStage = "autoSpinAttack";
+            if (this.autoSpinAttackTicks > 0) {
+                this.autoSpinAttackTicks--;
+                this.checkAutoSpinAttack(aabb, this.getBoundingBox());
+            }
+
+            webAiStepStage = "pushEntities";
+            this.pushEntities();
+            profilerfiller.pop();
+            webAiStepStage = "sensitive-water";
+            if (this.level() instanceof ServerLevel serverlevel1 && this.isSensitiveToWater() && this.isInWaterOrRain()) {
+                this.hurtServer(serverlevel1, this.damageSources().drown(), 1.0F);
+            }
+        } catch (Throwable throwable) {
+            if (BuildFlags.WEB_RUNTIME && webAiStepExceptionLogs < 30) {
+                webAiStepExceptionLogs++;
+                System.err.println(
+                    "[mc-web/living-aiStep] caught stage="
+                        + webAiStepStage
+                        + " class="
+                        + this.getClass().getName()
+                        + " tickCount="
+                        + this.tickCount
+                        + " error="
+                        + throwable.getClass().getName()
+                        + ": "
+                        + throwable.getMessage()
+                );
+            }
+            throw throwable;
         }
     }
 
@@ -3727,4 +3798,4 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
 
     public record Fallsounds(SoundEvent small, SoundEvent big) {
     }
-}
\ No newline at end of file
+}
